[project]
name = "desktop-organiser"
version = "0.1.0"
description = "Organises desktop files into file-type specific folders."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
]
