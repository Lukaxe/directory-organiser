# Directory Organiser

A tool to organise directories by sorting files into subdirectories based on their file extension.

## Setup

Create a `.env` file with the path to the folder you want to organise:

FOLDER_TO_BE_CLEANED=/path/to/your/folder



## Usage

```bash
uv run main.py
```